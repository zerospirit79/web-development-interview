 
1. В чем заключается суть ключевого слова global? Когда его применение целесообразно?

Используется для создания глобальной переменной. 

2. Какие суперглобальные переменные вы знаете? 

boolean – логический;
integer – целочисленный;
float – числа с плавающей точкой;
string – строка;
array – массив;
object – объект;
resource – особый тип данных, который содержит ссылки на внешние ресурсы (файлы, БД и т.д.);
NULL – пустота, отсутствие какого-либо значения.

3. Когда нужно использовать закрывающий дескриптор “?>“? 

Его нужно использовать всегда, это закрывающий дескриптор для скрипта PHP. 

4. Что выведет программа в каждом случае и почему?
function changeX($x){ 
   $x += 5;
   echo $x;
}  // Здесь программа не выведет ничего, нет значения у Х
$x = 1; 
echo $x; // программа выведет х=1 
changeX($x); // запускается функция
echo $x; // программа выведет 161 - первое значение х, следующее результат работы функции, последнее значие х

5. Что выведет программа в каждом случае и почему?
function test()
{
    static $a = 0;
    echo $a;
    $a++;
}
test();
test();
test();  

Результат работы функции будет 012 первоначальное значение и два результата работы функции прибавления ($a++;)

6. Как перевернуть массив? Есть массив array(‘h’, ‘e’, ‘l’, ‘l’, ‘o’), как из него получить array(‘o’, ‘l’, ‘l’, ‘e’, ‘h’)?
<?php

$arr = ['h', 'e','l','l','o'];
echo end($arr);
echo prev($arr);
echo prev($arr);
echo prev($arr);
echo prev($arr);

 ?>

 7. Как перевернуть строку задом наперед?

 <?php

echo strrev("Hello world!");

 ?> 
 вывод !dlrow olleH

 8. Что будет результатом работы данного кода?
$a=0;
if($b=$a)
    echo "One";
else
    echo "Two";

Результат работы if так как переменная b не задана выведется Two

9. Сгенерировать три случайных числа в диапазоне от 0 до 10. Если сумма этих чисел меньше 15, сгенерировать новую тройку.

<?php
$n=rand(1,10);
$a=rand(1,10);
$b=rand(1,10);
if ($n+$a+$b<15){
    $n=rand(1,10);
    $a=rand(1,10);
    $b=rand(1,10);
}
echo($n);
echo($a);
echo($b);

 ?>

10. Какое число выведет данный код?
$i = 10;
$i += ++i + $i + $i++;
echo $i; 
Данный код выведет ошибку во второй строке у ++i неверный синтаксис 
$i += ++$i + $i + $i++; - выведет 45

11. Что выведет приведенный ниже код?
$a = "1";
$a[$a] = "2";
echo $a;

данный код выведет 12 
